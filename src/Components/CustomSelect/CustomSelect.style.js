export const Style = (theme) => ({
  countrySelect: {
    width: "100%",
    margin: "10px 0",
    "& label": {
      fontSize: 20,
      color: `${theme.palette.primary.dark} !important`,
    },
  },
});
